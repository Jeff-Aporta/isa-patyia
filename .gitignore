.DS_Store
Thumbs.db
node_modules/
package-lock.json

# Componentes Jeff-Aporta: viven como submódulos en el monorepo Apps-fullstack,
# no aquí. Si reaparecen por accidente, no se versionan.
components/

# Tests locales: .mjs/.test.mjs/.test.ts que viven solo en este checkout.
# No se suben a Git. Para activarlos globalmente, moverlos a src/health/.
tests/

# Carpetas ocultas (prefijo .) — excepto workflows CI
**/.*/
!/.github/
!/.github/**